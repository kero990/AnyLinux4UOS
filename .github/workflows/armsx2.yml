name: Build ARMSX2 AppImage (ARM64) with version from tag

on:
  workflow_dispatch:          # 手动触发，会使用最新 Linux 标签


jobs:
  build:
    runs-on: ubuntu-24.04-arm
    container:
      image: ghcr.io/pkgforge-dev/archlinux:latest
    steps:
      - name: Setup anylinux environment
        uses: pkgforge-dev/anylinux-setup-action@0964f2258d6c93d1391359978dde081fd8b3c6af

      - name: Install base dependencies
        run: |
          echo "Installing package dependencies..."
          pacman -Syu --noconfirm shaderc qt6ct kvantum lxqt-qtplugin cmake clang ninja lld libwebp sdl3 base-devel git extra-cmake-modules libbacktrace qt6-tools kddockwidgets vulkan-headers vulkan-icd-loader ccache llvm ffmpeg perl
          echo "Installing debloated packages..."
          get-debloated-pkgs --add-common --prefer-nano

          echo "Installing yay..."
          echo "nobody ALL=(ALL) NOPASSWD: /usr/bin/pacman" >> /etc/sudoers
          chage -E -1 -I -1 -m 0 -M 99999 nobody
          git clone https://aur.archlinux.org/yay-bin.git /tmp/yay-bin
          cd /tmp/yay-bin
          chown -R nobody:nobody /tmp/yay-bin
          sudo -u nobody makepkg -si --noconfirm
          sudo -u nobody yay -S --noconfirm plutosvg-git rapidyaml

      - name: Clone ARMSX2 and determine version
        id: checkout
        run: |
          # 先克隆默认分支（macOS），后面再根据需要切换
          git clone --recursive -b macOS https://github.com/ARMSX2/ARMSX2
          cd ARMSX2

          # 获取所有标签
          git fetch --tags

          # 判断触发类型：是标签还是手动触发
          if [ "${{ github.ref_type }}" = "tag" ]; then
            # 推送标签触发：直接检出该标签
            echo "Triggered by tag: ${{ github.ref_name }}"
            git checkout ${{ github.ref_name }}
            # 提取版本号：从标签名去掉 "ARMSX2 linux " 前缀，例如得到 0.0.1
            VERSION=$(echo "${{ github.ref_name }}" | sed -nE 's/^ARMSX2 linux *v?([0-9]+\.[0-9]+\.[0-9]+).*$/\1/p')
            if [ -z "$VERSION" ]; then
              echo "Failed to parse version from tag, using 0.0.0"
              VERSION="0.0.0"
            fi
          else
            # 手动触发：查找最新的 Linux 标签
            echo "Manual trigger, finding latest Linux tag..."
            LATEST_TAG=$(git tag | grep -i 'linux' | sort -V | tail -n 1)
            if [ -n "$LATEST_TAG" ]; then
              echo "Checking out latest Linux tag: $LATEST_TAG"
              git checkout "$LATEST_TAG"
              VERSION=$(echo "$LATEST_TAG" | sed -nE 's/^ARMSX2 linux *v?([0-9]+\.[0-9]+\.[0-9]+).*$/\1/p')
            else
              echo "No Linux tag found, staying on macOS branch, using version 0.0.0"
              VERSION="0.0.0"
            fi
          fi

          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Detected version: $VERSION"

      - name: Build dependencies and compile
        run: |
          cd ARMSX2
          echo "Building FFmpeg and Qt dependencies..."
          chmod +x .github/workflows/scripts/linux/build-dependencies-qt.sh
          sed -i '/^echo "Building Qt Base..."/,/^cd \.\.$/s/^/#/' .github/workflows/scripts/linux/build-dependencies-qt.sh
          BUILD_FFMPEG=1 .github/workflows/scripts/linux/build-dependencies-qt.sh "$HOME/deps"

          echo "Downloading patches.zip..."
          wget https://github.com/isztldav/pcsx2_patches/releases/latest/download/patches.zip -O bin/resources/patches.zip

          cmake -B build -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INTERPROCEDURAL_OPTIMIZATION=ON \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_EXE_LINKER_FLAGS_INIT="-fuse-ld=lld" \
            -DCMAKE_MODULE_LINKER_FLAGS_INIT="-fuse-ld=lld" \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DENABLE_SETCAP=OFF \
            -DDISABLE_ADVANCE_SIMD=TRUE \
            -DUSE_LINKED_FFMPEG=ON \
            -DCMAKE_DISABLE_PRECOMPILE_HEADERS=ON

          ninja -C build install

      - name: Package AppImage with version
        run: |
          cd ARMSX2
          export OUTPATH=./dist
          echo "Packaging ARMSX2 version: $VERSION"

          quick-sharun \
            $PWD/bin/armsx2-qt \
            $PWD/bin/* \
            /usr/lib/libshaderc*

          cp bin/resources/ AppDir/bin/ -r
          cp bin/translations/ AppDir/bin/ -r
          cp bin/utils/ AppDir/bin/ -r
          cp bin/docs/ AppDir/bin/ -r

          quick-sharun --make-appimage

          # 重命名为带版本号的文件名
          if ls *.AppImage 2>/dev/null; then
            APPIMAGE_FILE=$(ls *.AppImage | head -1)
            NEW_NAME="ARMSX2-${VERSION}-linux-aarch64.AppImage"
            mv "$APPIMAGE_FILE" "$NEW_NAME"
            echo "Renamed to $NEW_NAME"
          fi

          # 移动到工作区根目录，方便后续上传
          cp *.AppImage $GITHUB_WORKSPACE/ || true

      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: ARMSX2-${{ env.VERSION }}-arm64
          path: "*.AppImage"

      - name: Create release (only on tags)
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          files: "*.AppImage"
          generate_release_notes: true
